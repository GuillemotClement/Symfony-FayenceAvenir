{% extends "base.html.twig" %}

{% block body %}

  <div class="block">
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button class="btn btn-primary">Réinitialiser mon mot de passe</button>
    {{ form_end(form) }}
  </div>

{% endblock %}