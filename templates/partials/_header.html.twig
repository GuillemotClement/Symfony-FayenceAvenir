<header class="navbar navbar-expand-lg ">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">
      <img class="logo" src="{{ asset('build/images/logo.png')}}" alt="logo">
    </a>
    <button class="navbar-toggler btn-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon txt-white"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a href="{{ path('articles_show')}}" class="nav-link txt-white">Articles</a>
        </li>
        <li class="nav-item">
          <a href="{{ path('event') }}" class="nav-link txt-white">Agenda</a>
        </li>
        <li class="nav-item">
          <a href="{{ path('presentation')}}" class="nav-link txt-white">l'Association</a>
        </li>
        <li class="nav-item">
          <a href="{{ path('contact') }}" class="nav-link txt-white">Nous contacter</a>
        </li>
      </ul>
      <div class="d-flex align-items-center justify-content-center">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {% if is_granted('ROLE_ADMIN') %}
          <a href="{{ path('administration')}}" class="btn btn-sm btn-outline-light me-2">Administration</a>
        {% endif %}
        <a href="{{ path('logout')}}" class="btn btn-sm btn-outline-light me-2">deconnexion</a>
        
        {# <a href="{{ path('edit_user', {id: app.user.id}) }}" class=""> #}
        <a href="" class="">
        {# Décommenter appel vers la bdd pour ajout image en prod #}
          <img class="rounded-circle avatar shadow hover-pic" src="{{ asset(app.user.picture) }}" alt="profil">
        </a>
      {% else %}
        <a href="{{ path('login')}}" class="btn btn-sm btn-outline-light me-3">Connexion</a>
        <a href="{{ path('register')}}" class="btn btn-sm btn-outline-light">Inscription</a>
      {% endif %}
      </div>
    </div>
  </div>
</header>
