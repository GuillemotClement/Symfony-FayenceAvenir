{% extends 'base.html.twig' %}
{% block body %}
  <div class="content">
    <h1>Nouvel article</h1>
    <div class="form-container">
      {{ form_start(form) }}
        {{ form_rest(form) }}
        <button>Valider</button>
      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}