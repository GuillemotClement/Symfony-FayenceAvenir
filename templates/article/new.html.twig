{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
  <div class="content">
    <h1>Nouvel article</h1>
    {{ form_start(form) }}
      {{ form_rest(form) }}
      <button>Valider</button>
    {{ form_end(form) }}
  </div>
{% endblock %}